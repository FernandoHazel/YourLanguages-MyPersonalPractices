<!DOCTYPE html>
<html lang="en">
    <%- include ('partials/head.ejs') %>
<body>

    <div style="padding: 200px;">

        <form method="post" action="/register-form/create" enctype="multipart/form-data"><!--enctype="multipart/form-data"-->

            <div class="form">
                <h2 class="text-center">Register</h2>
                <div style="display: flex; flex-direction: column;">

                    <input class="form-group" type="text" name="name" placeholder="Fernando" 
                    autofocus style="margin: 5px;" value="<%= locals.old && old.name %>" required>
                    <% if (locals.errors && errors.name) { %> 
                        <p class="feedback"><%= errors.name.msg %></p> 
                        <% } %>

                    <input class="form-group" type="text" name="lastName" placeholder="Ascencio" 
                    autofocus style="margin: 5px;" value="<%= locals.old && old.lastName %>" required>
                    <% if (locals.errors && errors.name) { %> 
                        <p class="feedback"><%= errors.name.msg %></p> 
                        <% } %>

                    <input class="form-group" type="text" name="email" placeholder="email@example.com"
                     autofocus style="margin: 5px;" value="<%= locals.old && old.email %>" required>
                    <% if (locals.errors && errors.email) { %> 
                        <p class="feedback"><%= errors.email.msg %></p> 
                        <% } %>

                    <input class="form-group" type="password" name="password" style="margin: 5px;" required>
                    <% if (locals.errors && errors.password) { %> 
                        <p class="feedback"><%= errors.password.msg %></p> 
                        <% } %>
                    
<!--Other important thing that we can add is a second password field and verify that both are the same-->
                    
                    <input type="file" name="avatar"/>

                    <!-- Option buttons to select account type -->
                    <label for="account">Account Type</label>
                    <label>
                        <input type="radio" name="userType" value="teacher" required> Teacher
                    </label>
                    <label>
                        <input type="radio" name="userType" value="student" required> Student
                    </label>

                    <label for="gender">Gender</label>
                    <label>
                        <input type="radio" name="gender" value="male" required> Male
                    </label>
                    <label>
                        <input type="radio" name="gender" value="female" required> Female
                    </label>
                    <label>
                        <input type="radio" name="gender" value="other" required> Other
                    </label>
                </div>

                <p class="remember">
                    <input type="checkbox" id="remember" name="remember" value="1" />
                    <label for="remember">Remember me</label>
                </p>
    
                <div class="form-group">
                    <input type="submit" value= "Register" class="btn btn-primary">
                </div>

                <p>Already have an account?</p>
                <a href="/login-form" class="btn btn-primary">Login</a>
            </div>
        </form>

        
    </div>

</body>
</html>