<!DOCTYPE html>
<html lang="en">
    <%- include ('partials/head.ejs') %>
<body>

    <div style="padding: 200px;">

        <form method="post" action="/register-form/create">

            <div class="form">
                <h2 class="text-center">Register</h2>
                <div style="display: flex; flex-direction: column;">
                    <input class="form-group" type="text" name="email" placeholder="email@example.com" autofocus style="margin: 5px;" value="<%= locals.old && old.email %>">
                    <% if (locals.errors && errors.email) { %> 
                        <p class="feedback"><%= errors.email.msg %></p> 
                        <% } %>
                    <input class="form-group" type="password" name="password" style="margin: 5px;">
                    <% if (locals.errors && errors.password) { %> 
                        <p class="feedback"><%= errors.password.msg %></p> 
                        <% } %>
                </div>

                <p class="remember">
                    <input type="checkbox" id="remember" name="remember" value="1" />
                    <label for="remember">Remember me</label>
                </p>
    
                <div class="form-group">
                    <input type="submit" value= "Register" class="btn btn-primary">
                </div>

                <p>Already have an account?</p>
                <a href="/login-form" class="btn btn-primary">Login</a>
            </div>
        </form>

        
    </div>

</body>
</html>